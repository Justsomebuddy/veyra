# P3-OG Machine-Pressure Module Memory

## Scope and status

- **Version:** 0.1.0
- **Facade:** `src.core.prime_power_observer_genesis_p3og`
- The implementation is an isolated, non-root-exported finite research
  candidate. It does not establish formation, an endogenous observer role,
  historical actualization, a theorem, certificate, object, or infinity.
- The flat `prime_power_observer_genesis_p3og*` files remain a cohesive module
  family; this documentation directory must not acquire `__init__.py` or alter
  Python import resolution.

## Invariants

- Validate exact outer types and declared tuple/text/integer budgets before
  nested traversal, UTF-8 encoding, decimal conversion, hashing, or replay.
  This includes dynamic enum/dataclass module, qualified-name, and field-name
  metadata used by the canonical codec. Exact dataclass field maps are capped
  before `dataclasses.fields()` materializes its tuple.
- Public machine operations reject a `REMOVED` boundary. Internal fixed-suffix
  replay may retain later terminal steps only as distinct, transition-budget-
  consuming removed-state receipts so matched schedules stay complete.
- Source/report validation freshly rebuilds canonical evidence; it is drift
  checking, not historical authentication.
- Keep `P3OG_NONCLAIMS`, the candidate status, and future obligations unchanged
  unless a separate proof-bearing design explicitly discharges them.

## Verification

Run Ruff and PyCompile on the eight module files, then both focused P3-OG test
files. Repository hygiene and publication gates remain separate; focused tests
must not be described as full verification.
