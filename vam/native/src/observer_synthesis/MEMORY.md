# Observer Synthesis Module

- V5 task semantics evaluate every candidate on the task's bound
  `surface_states`; the misrepresentation-recovery row uses a nonidentity
  affine permutation and must not collapse to the hidden-affine task.
- A found V5 ledger evaluates the complete incumbent-cost frontier. Only the
  strictly higher-cost suffix is pruned, and the independent verifier derives
  that frontier again from the request, benchmark, and catalog.
- V1–V4 byte contracts are frozen. V5 digest changes require exact focused
  regression evidence and synchronized documentation.
- All changed functions retain bounded opt-in diagnostics for entry, exit,
  rejection/error, and material search state transitions.
